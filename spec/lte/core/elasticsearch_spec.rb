require 'spec_helper'

describe Lte::Core::Elasticsearch do
  it 'has a version number' do
    expect(Lte::Core::Elasticsearch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
